{
  "name": "go-grpc",
  "context": "..",
  "dockerFile": "./Dockerfile",
  "overrideCommand": false,
  "customizations": {
    "vscode": {
      "extensions": [
        "halon.hsl-syntax",
        "halon.vscode-halon",
        "halon.hsl-linter",
        "halon.hsl-debug",
        "golang.go"
      ]
    }
  },
  "mounts": [
    "source=${localWorkspaceFolder}/.devcontainer/smtpd/dist,target=/etc/halon,type=bind,consistency=cached"
  ],
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/usr/local/go/bin:/root/go/bin/"
  },
  "build": {
    "args": {
      "HALON_REPO_USER": "${localEnv:HALON_REPO_USER}",
      "HALON_REPO_PASS": "${localEnv:HALON_REPO_PASS}"
    },
    "options": ["--platform=linux/amd64"]
  }
}